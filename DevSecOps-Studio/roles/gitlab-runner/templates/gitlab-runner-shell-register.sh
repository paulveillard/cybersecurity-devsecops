export GODEBUG=netdns=cgo && gitlab-runner register --non-interactive --url '{{ gitlab_runner_coordinator_url }}' --registration-token '{{ gitlab_runner_registration_token }}' --name '{{ gitlab_runner_name }}' --tag-list '{{ gitlab_runner_tags | join(",") }}' --executor shell --builds-dir '{{ gitlab_runner_persistent_dir }}/builds'  --cache-dir '{{ gitlab_runner_persistent_dir }}/cache'
